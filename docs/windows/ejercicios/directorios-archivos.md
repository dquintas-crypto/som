# 🧠 Ejercicios prácticos 

## 🗂️ 1. Navegación y Directorios

| Objetivo | Comando |
|-----------|----------|
| Mostrar el contenido de la carpeta actual | <span class="hint" onclick="toggleMensaje()">💡</span> |
| Mostrar solo los nombres de archivos sin detalles | |
| Mostrar el contenido de una carpeta grande, deteniendo cada pantalla | |
| Mostrar solo los archivos `.exe` del directorio actual | |
| Mostrar el contenido ordenado por fecha (más recientes primero) | |
| Ver todos los elementos, incluidos ocultos y de sistema | |
| Mostrar solo las carpetas (no archivos) | |
| Buscar archivos `.txt` en la carpeta actual y subcarpetas | |
| Crear una carpeta llamada `Proyectos2025` | |
| Crear una carpeta llamada `Informes` dentro de `Proyectos2025` en un solo comando | |
| Ir a la carpeta `C:\Users` | |
| Volver al nivel anterior | |
| Ir al directorio raíz | |
| Eliminar la carpeta `temporal` | |
| Eliminar la carpeta `C:\Proyectos\2023` y todo su contenido sin confirmación | |
| Mostrar la estructura de carpetas completa de `C:\Proyectos` incluyendo archivos | |
| Mostrar la estructura de carpetas del directorio actual en forma de árbol | |


## 🗃️ 2. Archivos

| Objetivo | Comando |
|-----------|----------|
| Copiar el archivo `informe.txt` desde `C:\Datos` a `D:\Backup` | |
| Copiar todos los archivos `.docx` a la carpeta `E:\Documentos` verificando la copia | |
| Copiar todos los `.txt` de la carpeta actual al directorio `D:\Textos` sin pedir confirmación | |
| Mover todos los archivos `.jpg` desde `C:\Fotos` a `E:\Imagenes` | |
| Mover todos los archivos `.log` desde `C:\Temp` a `D:\Logs` sin pedir confirmación | |
| Eliminar todos los archivos con extensión `.tmp` de `C:\Temp` y sus subcarpetas | |
| Eliminar todos los archivos `.bak` sin pedir confirmación | |
| Cambiar el nombre de `reporte.txt` a `reporte_2025.txt` | |
| Cambiar la extensión de todos los `.log` a `.txt` | |
| Mostrar el contenido del archivo `errores.txt` pausando la salida | |
| Asignar el atributo de solo lectura a `datos.conf` | |
| Quitar el atributo de oculto al archivo `manual.pdf` | |
| Crear una copia exacta de `notas.txt` llamada `copia_notas.txt` | |
| Añadir el contenido de `log1.txt` al final de `log_total.txt` | |

---

## 💪 3. Retos combinados

1. Crea una carpeta llamada `EjerciciosCMD` en tu escritorio, entra en ella y dentro crea otra llamada `Pruebas`.
2. Dentro de `Pruebas`, crea tres archivos vacíos llamados `uno.txt`, `dos.txt` y `tres.txt` (puedes usar `echo` o `type nul >`).
3. Copia todos los `.txt` a una nueva carpeta llamada `Copias`.
4. Cambia el nombre de `tres.txt` a `final.txt`.
5. Elimina todos los archivos `.txt` de la carpeta `Pruebas` sin pedir confirmación.
6. Muestra el árbol completo de la carpeta `EjerciciosCMD` con todos los archivos.



> 💡 **Consejo:** puedes guardar tus respuestas en un archivo `.cmd` o `.txt` y probarlas una a una en la terminal de Windows.



